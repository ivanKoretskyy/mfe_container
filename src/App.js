import React from 'react';
import { mount } from 'marketing/MarketingApp';

console.log('Container App', mount);
export default () => {
  return (
    <div>
        <h1>Container App</h1>
    </div>
  );
};
